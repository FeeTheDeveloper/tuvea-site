# PLAY A — Repo Architect (Foundation Builder)

Goal:
Lock the UVEA foundation and layout to BRAND_LOCK while keeping the code build-safe.

Deliverables:
- app/layout.tsx implements 2-panel layout + loads Cinzel/Inter
- Global CrisisBanner included in layout
- Navbar + Footer shared components
- Tailwind theme includes uvea.* colors
- No bg-white or light page backgrounds
- Routes exist: /, /about, /programs, /resources, /get-help, /donate, /partner, /success-stories, /contact, /ai (placeholder)
- Seal references /public/brand/uvea-seal.webp (user will add the asset)

Finish:
- Ensure npm run dev works
- Summarize files changed
